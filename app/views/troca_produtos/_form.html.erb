<%= form_with(model: troca_produto) do |form| %>
  <% if troca_produto.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(troca_produto.errors.count, "error") %> prohibited this troca_produto from being saved:</h2>

      <ul>
        <% troca_produto.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :cliente_id %>
    <%= form.select :cliente_id, @clientes.collect {|p| [p.nome, p.id] }, {include_blank: true} %>
  </div>

  <div class="field">
    <%= form.label :produto_id %>
    <%= form.select :produto_id, @produtos.collect {|p| [p.nome, p.id] }, {include_blank: true} %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
